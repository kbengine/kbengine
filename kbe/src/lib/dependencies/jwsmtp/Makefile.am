# Top directory of jwSMTP library source tree

SUBDIRS = docs jwsmtp

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = jwsmtp-@JWSMTP_API_VERSION@.pc

extras = mail.dsw mail.dsp lib.dsp History license.txt attach.png

EXTRA_DIST = $(extras)
